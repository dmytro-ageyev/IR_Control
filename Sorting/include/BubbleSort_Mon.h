/**
 * @file BubbleSort.h
 * @brief Заголовковий файл з оголошенням функції сортування масиву методом «Бульбашки».
 *
 * Цей файл містить прототип функції BubbleSort(), яка сортує масив цілих чисел
 * за зростанням методом «Бульбашки». Можна підключати цей заголовковий файл до
 * основного файлу програми (.ino або .cpp) для використання функції сортування.
 *
 * Особливості:
 * - Сортування масиву на місці без використання додаткових масивів.
 * - Простий і зрозумілий алгоритм для навчальних і демонстраційних цілей.
 * - Можна комбінувати з іншими функціями, наприклад, для виведення масиву
 *   через серійний монітор.
 *
 * @note Для правильного підключення файлу рекомендується використовувати
 * захист від повторного включення (#ifndef / #define / #endif).
 *
 * @author Дмитро Агеєв
 * @date 07.10.2025
 */

#ifndef BUBBLESORT_H     // Перевіряємо, чи цей заголовковий файл уже не підключено
#define BUBBLESORT_H     // Якщо ні — визначаємо макрос, щоб запобігти повторному включенню

#include <Arduino.h>     // Підключаємо бібліотеку Arduino (для типу String, Serial тощо)

// Оголошення (прототип) функції сортування методом "бульбашки"
// Параметри:
//   arr[] – масив цілих чисел, який потрібно відсортувати
//   size  – кількість елементів у масиві
void BubbleSort_Mon(int arr[], int size);

#endif  // BUBBLESORT_H
